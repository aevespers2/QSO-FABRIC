# QSO Security

Implementations fail closed on malformed canonical data, duplicate keys, unsupported critical fields, unresolved required references, unsafe package paths, invalid signatures, stale base hashes, and authorization failures. Validators never execute embedded code. Constitutional, consent, identity-continuity, and provenance controls outrank lower-authority policies.
