# Interoperability

Implementations advertise supported formats, schema major versions, profiles, canonicalization, hashes, signatures, and package limits. A sender must not downgrade critical protections. Unknown required components fail negotiation; unknown optional components may be preserved as opaque hash-verified resources.
