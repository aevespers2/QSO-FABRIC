# Versioning

Formats and schemas use semantic versioning. Unknown critical fields cause rejection; unknown non-critical fields are preserved. Migration is explicit, provenance-recorded, and never silently rewrites signed source objects.
